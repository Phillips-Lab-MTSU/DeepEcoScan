User 
    id
    project(s)
        id
      <-User
        files
            id
            name
          <-project
        job
          <-project
          id

To determine
results (probably in jobs)
data files (probably in jobs as well)

if a job is underway then the information tethered to the job shoukld be unmodifyable until 
it is complete or it fails

follow CRUD for logic in database
bucket will hold hash blobs, that will allow the retrieval of file information in the database

find out if there is a way to generate unique keys for records within the database on Mongos side
for changing how we pull the information from the database for the dataprep of setbert, look at
 pulling one or two of the needed files at a time. more efficient!

find out how to display a progress bar for the program working on a dataset
    we may be able to leverage the data progress bars in setbert currently 
    there and supply the progress to job record in schema
    


================================================
How to use mongoose

brew install mongoose
